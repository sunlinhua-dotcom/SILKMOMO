# 断点续档 · 更新至 2026-07-13（切账号 / 换模型续接）

> 用途：把「对话态」交给没有本次上下文的新会话。本机 memory（`MEMORY.md` + `memory/*.md`）、
> `CLAUDE.md`、`CONTEXT.md`、git 历史照常自动加载，本文件**只补增量**。读完本文件 +
> `CLAUDE.md` + `MEMORY.md` 即可接手。

## 0. 恢复第一步（照做）

1. `git status` + `git log -5 --oneline` 核对：当前在 **`main`**，HEAD 应是 **`aa6cef6 feat(size): 新增出图尺寸 INS 3:4`**。**本地 main = origin/main，之前所有功能分支都已合并、已 push、已随 Zeabur 部署生产**（silkmomo.digirepub.com）。没有待合并的功能分支。
2. **红线**：`new0701/` 与根目录 `0707silkmomo需求整理.pptx`（~277MB 客户素材）已 `.gitignore`——别 `git add -A`、别提交它们。`.claude/settings.local.json` 故意不提交（本地工具配置，长期 modified 正常）。`0709.md`（本文件）未跟踪，不必提交。
3. 检查命令（本会话都复跑过绿）：`npx tsc --noEmit`、`npm run build`（✓ Compiled）、`npm run lint`（0 error，仅 12 个既有 `<img>` warning）。本地 dev：`npm run dev`，端口 **4605**；preview_start 配置名 `silkmomo-dev`。⚠️ preview 浏览器**本会话实测未登录**（登录页），够不到受 JWT 保护的页面；要真机验证需用户自己登录，别去输密码。
4. 真实出图打样不走 UI（preview 传不了文件）：用 `refs/e2e/` 里的探针脚本直连 API（见 §4）。开工前先跟用户确认从哪条继续。

## 1. 2026-07-13 会话做了什么（全部已合并 main、已部署生产）

1. **换脸稳定真实出图打样通过 → 合并上线**（`9fc390a`）。客户投诉「组图换装模特脸被产品图真人带偏」的修复（肖像卡身份锚 + 参考图顺序 + 提示词忽略产品图人脸），程序化打样：换装 swap（Gemini 3/3、GPT 2/3，1 张 GPT 撞 280s 超时**自动退款**正确）+ 同景换品 products（Gemini 2/2），**7 张成功输出全是各自肖像卡里那个全新一致模特、无一被带偏**，肖像卡都先出、走 Gemini、不计费。资金扣/退全对。通过后把 `feat/lookbook-identity-anchor` FF 合并进 main 并 push（`aa48069 → 9fc390a`）。
2. **GPT 生图 503「no available channels」根因定位并解决（非代码 bug）**。上游是 apiyi（`api.apiyi.com`），按「令牌分组 → 通道」路由。报 `Current group image2Enterprise has no available channels for model gpt-image-2-all`——令牌当时挂在 `image2Enterprise` 分组、该分组没配 `-all` 通道。**用户在 apiyi 后台把令牌切到 `default` 分组即解决**（分组是令牌级属性，本地/线上共用同一把 sk-ACN 都生效）。修后稳定性压测 8/8 成功、延迟 57–89s、0 超时 0 无通道。⚠️ image-backends.ts:341 的 503 重试是**重试同一模型**，对「无可用通道」这种确定性路由失败无效，真出这错去查分组别指望自愈。
3. **模特脸真实感 + 参考图光线匹配**（`add3c15`，已部署）。生成的脸太完美/AI 味重（磨皮无瑕）。新增 export 常量 **`FACE_REALISM_DIRECTIVE`**（lib/api.ts）：皮肤真实质感（毛孔/细纹/淡雀斑/自然色差）+ 禁磨皮美颜/蜡质 CGI 脸/重调色 + 少妆 + 真人未修图 editorial；并含**光线「参考图优先」**——有场景/背景/参考图就复刻其光向/软硬/色温/明暗给模特打光（避免硬编码日光和场景打架），无参考才回退日光。注入四个出脸构造器：肖像卡（route.ts `buildSceneGroupPortraitPrompt`，需 import 常量）+ `buildProductShotPrompt` + `buildSceneShotPrompt` + `buildSceneGroupPrompt`；另强化 product 的 bgRef、scene 的 sceneRef 两分支「按参考图给模特打光」。**想再调真实感/光线只改这一个常量，四处全生效。** 双引擎双验证（换装 + 场景光线匹配，Gemini+GPT）均通过。**取舍：加 lived-in 质感后模特显得成熟几岁（二十五六→三十出头）**，要更年轻在常量里加年龄倾向即可。
4. **新出图尺寸 `INS 3:4 · 1080×1440`**（`aa6cef6`，已部署）。lib/models.ts 的 `PRODUCT_OUTPUT_SIZES` 与 `SCENE_OUTPUT_SIZES` 各加一项 `ins_3_4`（aspectRatio `3:4`），自动出现在所有尺寸选择器（产品/场景/lookbook/批量）。⚠️ 全系统**不按配置宽高裁切**——后端按比例出图、分辨率随引擎原生（Gemini ~2K、gpt-image 1024×1536），所有预设宽高都是**名义标注**。用户明确选了「不要精确像素」，故没加缩放步骤；若日后要精确 1080×1440 导出，需另加出图后 resize（引入图像处理）。

## 2. 环境坑 / 红线（不是 bug，别去"修"）

- **apiyi 令牌分组**：GPT 出图 503「no available channels」先查令牌在 apiyi 是不是挂对了有该模型的分组（见 §1-2），别改代码。
- **GPT 模型 = 令牌 × 型号名**：`OPENAI_IMAGE_API_KEY` 配了就优先用它且默认 `gpt-image-2`；不配则回退主令牌 `GEMINI_API_KEY` 默认 `gpt-image-2-all`。`OPENAI_IMAGE_MODEL` 设了强制型号。本地 `.env.local`：注释掉独立令牌 + `OPENAI_IMAGE_MODEL=gpt-image-2-all`（走主令牌 sk-ACN，分组已在 default，支持 -all）。`.env.local` gitignore、不随提交走。
- **GPT 慢**：`gpt-image-2-all` ~60–235s/张，单张偶发 280s 超时（会自动退款）；组图/产品 GPT 路径已按 ≤3/请求分块。
- 存储键红线：`SilkMomoDB` 库名、`silkmomo_*` localStorage 键、`silkmomo_token` cookie **严禁改名**；Dexie 不 bump version（本会话新增字段/尺寸 id 都只是值，未动 schema）。
- 资金红线：`deductBalance` 成功后到 inner try 之间**必须无 await**；退款非幂等。
- push main 即触发 Zeabur 自动部署生产——对外不可逆，改生图相关的东西合并前先真实出图验过。

## 3. 下一步 / 待办（挑一条，开工前跟用户确认）

1. **线上冒烟**（用户侧）：三笔部署（`9fc390a`/`add3c15`/`aa6cef6`）几分钟内随 Zeabur 上线，登录 silkmomo.digirepub.com 出一张确认真实感/新尺寸生效。我这边看不到 Zeabur 状态。
2. **真实感想更年轻**：改 `FACE_REALISM_DIRECTIVE`（lib/api.ts）加一句年龄倾向（如「mid-20s, youthful but real」），四处提示词同步。
3. **精确 1080×1440 像素导出**：当前只按 3:4 比例出图、非精确像素；如需真 INS 规格文件，加出图后 resize 步骤。
4. （二期，客户 PPT `0707silkmomo需求整理.pptx`）衍生镜头模板：去头特写（唇/颈/肩）、腰部/领口、对镜自拍×5 变体、进阶多模特多产品——创作型输出，先对齐预期再做。

## 4. 打样探针（refs/e2e/，均 gitignored，直连 API 不走 UI）

- `lookbook-swap-probe.mjs <gemini|openai>` — 换装（N景1品）：3 产品图 + 3 场景图，出肖像卡锚 + N 换装图存盘。
- `lookbook-products-probe.mjs <gemini|openai>` — 同景换品（1景N品）：1 场景 + N 产品组。
- `scene-light-probe.mjs <gemini|openai>` — 场景图光线匹配：1 产品 + 1 场景参考图，验模特是否被打成场景同款光。
- `gpt-stability-probe.mjs [N]` — `gpt-image-2-all` 稳定性压测：连打 N 次真实 edits，量成功率/延迟/503/超时。
- `probe-gpt.mjs` — apiyi 模型可用性探针（/v1/models + gemini + gpt-image-2-all/2/1）。
- 跑法：`BASE=http://127.0.0.1:4605 node refs/e2e/<脚本>.mjs gemini`（用 127.0.0.1，Node fetch 对 localhost 会走 IPv6 ::1 打不通）。素材在 `refs/product-photos/`（蓝条纹睡衣带真人脸 = 偏移来源、白贵宾、洗衣房场景）。探针用 admin 令牌（id `cmomfoj5c0000juzvr1bc9x11`）铸 JWT，余额不足会自动充值；出图存 scratchpad。

## 5. 验证口径

- `tsc` / `build` / `lint`：本会话每次改动后都复跑过，全绿。
- 真实出图：换脸稳定（swap+products，双引擎）、真实感+光线匹配（双引擎）、GPT 稳定性（8/8）——均实测通过并已部署。
