# 断点续档 · 2026-07-18（切账号 / 隔日续接，整合版）

> 用途：SILKMOMO 生图链路。本机 memory（MEMORY.md + memory/*.md）与账号无关、自动加载，
> 本文件只补对话态。**本次 0718 会话除写本文件外没做任何事**——7/17 12:20（微信版客户信
> 落盘）之后项目零动作，git 无新提交（今日核实 HEAD=origin/main=409611c）。
> 旧交接：`0709.md` 已过时勿参照；`0717.md` 已归档但其 **§6（真图验证细节）/ §7（两个新
> 缺陷 + 代码误读纠正）仍然有效**，本文件只留结论，细节需要时去那里查。

## 0. 恢复第一步（照做）
1. `git status` + `git log -5 --oneline` 核对：HEAD 应是 `409611c`，与 origin/main 一致（2026-07-18 已核）。
2. **红线**：根目录 `07*.md`、`客户更新-0717*`、`verify-0717-出图验证/` 都是工作产物，**不提交**；worktree `angry-edison` 里的 `ld.json`/`ld2.txt` 是调研垃圾，别当项目文件；**别删任何 `claude/*` 分支或 `.claude/worktrees/*`**（可能是活的后台 session；若它们开 PR，关掉别合——今日核实当前无任何 open PR）。
3. 主仓 `.env.local` 含真实 302 key（今日核实存在），勿入库勿外泄。
4. 从 §4 挑一条，**开工前先跟用户确认**。

## 1. 最近做了什么（全在 7/17，已有记录，此处只挂指针）
- 代码三连（均已合并上线）：#3 通道切 302 官转+画质三档、#4 客户五条规则重写提示词、#5 余额校验前置 → `0717.md §1` + memory。
- **#4 提示词真图验证通过**（五条规则全达标、跑的是仓库真实代码、无需改码）→ `0717.md §6` + memory `project_prompt_v4_realimage_verified`。
- 写客户更新信过程中挖出**两个未修真缺陷**（§4.1/4.2）+ 纠正一处我方代码误读（整批重跑其实吃补图规则，`route.ts:797` 带锚即真）→ `0717.md §7`。
- 产出客户信两版：`客户更新-0717.md`（详版）+ `客户更新-0717-微信版.txt`（12:20 成稿，可直接粘贴发送，末尾请客户重点复测「去头景别 + 多套产品连跑」）。

## 2. 当前 git / 工作区状态（2026-07-18 核实）
- `main` = `409611c` = origin/main = 线上（Zeabur 自动部署，7/16 deployments success 硬证据见 `0717.md §6`）。
- 未提交：`.claude/settings.local.json`(M，权限配置含 .env 脱敏 sed，非项目代码，随用户处置)；未跟踪 5 项全是工作文档（§0.2 红线清单）。
- 待处置分支：`feat/lookbook-identity-anchor`（lookbook 换脸+组图，**等用户 go 才合并**，见 memory）；`fix/scene-group-preflight-0717`（已并入 main，其 worktree `angry-edison` 可弃但删前问用户）。

## 3. 必须知道的环境坑（非产品 bug，别去「修」）
- 本地 Postgres 只有测试数据；生产 DB 在 Zeabur 内网，**本机查不了线上失败记录**。
- zeabur CLI x509 证书错误用不了；部署=推 main 自动触发；看 Zeabur 后台需用户自己的 Chrome。
- 线上/预览受 JWT 登录门控；7/17 时预览浏览器登录态已失效，验证前需用户先登录（memory 里「preview 带登录态」那条已过时）。
- **验证产品链路生图必须走仓库真实代码打 302**（如 7/17 的 dynamic import 方式），**不要**套全局 CLAUDE.md 的 OpenClaw 端点——那是给通用生图用的，测的不是产品代码。
- 给 302 传大参考图（实测 1.37MB PNG）会 280s 超时挂起，转小 JPEG 就过——细节与不确定性见 `0717.md §7.2`，别当已定结论。

## 4. 下一步（挑一条，开工前跟用户确认）
1. **发客户信**：微信版已成稿，**是否已发给客户磁盘上看不出来——先问用户**。信里两个只有用户能拍板的事：①老客户高清涨价要不要补偿（信里写的是「觉得不合理直接跟我说」）②若客户追问高清 ¥3.50 定价理由，需用户自己答。
2. **尺寸标注不实**（`0717.md §7.1`）：`ins_3_4` 标 1080×1440 但 GPT 侧实际出 1024×1536=2:3，比例就不对；`ins_vertical` 标 4:5 内部记 3:4。修法二选一：改标注为比例预设去掉像素数，或 GPT 侧出图后裁切。入口 `lib/models.ts:297-308` + `lib/image-backends.ts:269-278`。
3. **大参考图超时正交实验**（`0717.md §7.2`）：体积/格式/像素三变量分离 + 在真实页面上传路径复现一次（前端压缩上限 1.5MB > 实测超时的 1.37MB，有真实风险敞口）。入口 `lib/image-compressor.ts` + `components/ImageUploader.tsx:75`。
4. **lookbook 合并**：`feat/lookbook-identity-anchor` 打样已过、等用户说 go。
5. **（用户明确暂缓，勿主动）** 302 key + `JWT_SECRET` 轮换。

## 5. 验证口径（分清「已跑」与「仅记录」）
- 今日（7/18）实跑：`git status`/`log`/worktree/`gh pr list`（无 open PR）/`.env.local` key 存在性——仅这些。
- 7/17 记录：302 通道端到端真出图✅；#4 真图验证✅（详 `0717.md §6`）；tsc/build/lint 各提交时绿，**此后未复跑**（lint 有 12 条既有 `<img>` warning 非新引入）。
- 始终未做：#5 余额预检的运行时验证（代码层四项过）。
