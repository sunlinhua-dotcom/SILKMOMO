# 断点续档 · 2026-07-21（切账号 / 隔日续接）

> 用途：SILKMOMO 生图链路。本机 memory（MEMORY.md + memory/*.md）与账号无关、照常自动加载，
> 本文件只补「对话态」。读完本文件 + MEMORY.md 即可接手。
> 旧交接：`0709.md`/`0717.md`/`0718.md` 已归档；0718 里仍有效的部分已并入本文件 §4。
> 本次会话核心决策与批次 A 详情另存 memory `project_lookbook_batcha_0721.md`（自动加载，别重查）。

## 0. 恢复第一步（照做）
1. `git status` + `git log -5 --oneline` 核对：HEAD 应是 `b822968`（组图换装批次 A），= origin/main = 线上（Zeabur 部署 success 已于 7/21 21:09 经 gh api deployments 硬确认）。
2. **红线**：根目录 `07*.md`、`LOG.md`、`Progress.md`、`bugs.md`、`客户更新-0717*`、`verify-0717-出图验证/` 都是工作文档，**不提交**；`.claude/settings.local.json` 是本地权限配置，不入库；主仓 `.env.local` 含真实 302 key，勿入库勿外泄；**别删任何 `claude/*` 分支或 `.claude/worktrees/*`**。
3. 验证命令：`npx tsc --noEmit`、`npm run lint`（12 条存量 `<img>` warning 非新增）、`npm run build`（真机 Turbopack 可跑；**Codex 沙盒里 Turbopack 起进程/绑端口会被禁**，沙盒内用 `npx next build --webpack` 替代）。
4. 从 §4 挑一条，开工前跟用户确认。

## 1. 最近做了什么（7/19–7/21，均已 commit + 上线）
- 7/19：`ffcb255` 尺寸诚实化、`7284630` 参考图三层防御（上次续档后的两修，详见 memory `project_size_timeout_fixes_0719`）。
- 7/21 本会话（客户 Yukichai 反馈：组图场景参考失效+AI 感+频繁 network error (TypeError)）：
  - Codex 两轮只读调查钉根因：①单次调用 scene-base 1 张被 8 张产品全身照压制 + prompt 未叫忽略产品图背景；②GPT 组图单条 SSE 串 3 张可挂 10+ 分钟被网关掐断，前端裸透 TypeError。
  - **老板拍板产品语义（铁律）**：组图=一张一张对应；产品参考图只提供服装；**每张场景主图各出 1 张**（保留其姿势/构图/氛围只换衣服）；上传两步走（①产品参考图②场景主图）。不做映射模式选择、不做自动决策。
  - 批次 A 五项实现并上线 `b822968`：prompt 强化（garment-only + scene-base 唯一权威，#4 五条规则未动）、GPT 组图分块 3→1（块间自动接续）、报错人话化（引导「生成剩余 N 张」）、product/scene_ref 按 id 显式排序、/lookbook 两步走上传+「共将生成 N 张」。
  - 改动文件：`lib/api.ts`（buildSceneGroupPrompt）、`app/task/[id]/page.tsx`、`app/lookbook/page.tsx`。tsc/lint/真机 build 全绿；prompt 与分块 diff 已逐行人审。

## 2. 当前 git / 工作区状态
- HEAD = `b822968`（7/21），分支 `main`，与 origin 同步，线上已部署。
- 未提交：`.claude/settings.local.json`(M)；未跟踪 9 项全是工作文档（§0.2 红线清单 + 本文件）。
- 待处置分支：`feat/lookbook-identity-anchor`（换脸+组图打样已过，**等用户 go 才合并**）；worktree `angry-edison` 可弃但删前问用户。

## 3. 必须知道的环境坑（不是产品 bug，别去「修」）
- Codex MCP：必传 `cwd`；`sandbox` 每次调用可覆盖（改码传 `workspace-write`）；沙盒 `.git` 只读→**commit/push 由 Claude 亲自做**；沙盒内 Turbopack build 必失败（起进程被禁），属沙盒限制非代码问题。
- zsh 脚本坑：`status` 是只读变量，后台轮询脚本用它命名会直接炸（本会话踩过，改名 `dep_state` 解决）。
- 部署确认走 `gh api repos/sunlinhua-dotcom/SILKMOMO/deployments` 最硬；zeabur CLI x509 用不了；生产 DB 在 Zeabur 内网本机查不了；线上/预览受 JWT 登录门控，验证前需用户先登录。
- git 提交身份未全局配置（commit 会警告 auto-configured identity），无碍但别 amend 别人的提交去「修」它。
- 验证产品链路生图必须走仓库真实代码打 302，**不要**用全局 CLAUDE.md 的 OpenClaw 端点（那测的不是产品代码）。

## 4. 下一步（挑一条，开工前跟用户确认）
1. **等客户复测批次 A**（最优先）：用出问题那单（8 产品+3 场景）重跑，验三点——场景是否保留影棚氛围、两步走上传是否清晰、报错是否变少变可读。复测结果决定要不要上批次 B。
2. **批次 B（backlog，已画在飞书画布）**：场景忠实度仍不足→「场景底图/氛围参考」角色拆分；断线自动补生成（**必须先做幂等防重复扣费**）；服装混款检测黄条告警（搭现有服装分析调用，只提醒不代决策）。
3. **批次 C（大改根治）**：服务端任务队列 jobId+轮询、图先落服务端，断网刷新不丢。
4. 0718 遗留：①客户信（`客户更新-0717-微信版.txt`）**是否已发不明——先问用户**；②lookbook 分支合并等用户 go；③（用户明确暂缓勿主动）302 key + JWT_SECRET 轮换。

## 5. 验证口径（分清「本次实跑」与「仅记录」）
- 7/21 实跑：tsc ✅ / lint ✅（12 存量 warning）/ 真机 `npm run build` ✅ / 部署 success（gh api，21:09）——均针对 `b822968`。
- 仅记录未复跑：#4 提示词真图验证（7/17 绿）；#5 余额预检运行时验证始终未做。
- **批次 A 的效果验证（场景忠实度、报错频率下降）完全未做**——上线≠有效，等客户复测或自行真图打样。
